export class device {
  constructor(control_id, user_id, mode, pump_status) {
    this.control_id = control_id;
    this.user_id = user_id;
    this.mode = mode;
    this.pump_status = pump_status;
  }
}
